{
  "workspace": "bullet-farm",
  "tests": {
    "AGENTS.md": {
      "command": "just fast",
      "purpose": "verify root routing remains visible"
    },
    "SPLIT.md": {
      "command": "just fast",
      "purpose": "verify the split-family boundary remains explicit"
    },
    "LICENSE": {
      "command": "just fast",
      "purpose": "verify the public hub metadata remains complete"
    },
    "Justfile": {
      "command": "just fast",
      "purpose": "verify public proof recipes still route to executable local lanes"
    },
    ".gitignore": {
      "command": "just fast",
      "purpose": "verify generated local state remains outside source authority"
    },
    ".fusion/": {
      "command": "just contract-check",
      "purpose": "verify fusion output remains derived local state"
    },
    ".jankurai/": {
      "command": "just audit",
      "purpose": "recreate ignored audit reports from the pinned local auditor"
    },
    "README.md": {
      "command": "just fast",
      "purpose": "verify the public onboarding page stays complete"
    },
    "CHANGELOG.md": {
      "command": "just audit",
      "purpose": "verify release language remains evidence-backed and honestly blocked"
    },
    "agent/": {
      "command": "just fast",
      "purpose": "verify manifests remain parseable"
    },
    "docs/": {
      "command": "just fast",
      "purpose": "verify spec corpus and architecture docs remain routed"
    },
    "docs/release.md": {
      "command": "just audit",
      "purpose": "verify the public release contract remains evidence-backed and fail-closed"
    },
    "contracts/": {
      "command": "just contract-check",
      "purpose": "reject canonical schema-bundle and generated binding drift"
    },
    "crates/": {
      "command": "cargo test --locked -p bullet-wire",
      "purpose": "prove canonical cross-repository wire identities and encodings"
    },
    "fixtures/": {
      "command": "cargo test --locked -p bullet-wire --test canonical_hostile --test policy_registry",
      "purpose": "prove hostile byte refusal and forensic source preservation"
    },
    "formal/": {
      "command": "just model-check",
      "purpose": "check exactly two pinned bounded models and their locked state counts"
    },
    "ops/": {
      "command": "just fast",
      "purpose": "verify local CI parity scripts"
    },
    "release/": {
      "command": "cargo test --locked",
      "purpose": "verify release signer and family-lock policy"
    },
    "policy/": {
      "command": "cargo test --locked -p bullet-wire --test policy_registry",
      "purpose": "prove strict policy decoding, registry completeness, and offline safety"
    },
    "scripts/": {
      "command": "just check",
      "purpose": "verify fuse and onboarding scripts"
    },
    "src/": {
      "command": "cargo test --locked",
      "purpose": "verify the family coordination and onboarding CLI"
    },
    "tests/": {
      "command": "cargo test --locked",
      "purpose": "verify coordination races and fail-closed behavior"
    },
    "tests/doctor.rs": {
      "command": "cargo test --locked --test doctor",
      "purpose": "prove standalone-hub diagnosis remains read-only and fail-closed"
    },
    "tests/release_bundle.rs": {
      "command": "cargo test --locked --test release_bundle",
      "purpose": "prove the five-platform release manifest, bytes, and detached signatures fail closed"
    },
    "Cargo.toml": {
      "command": "cargo test --locked",
      "purpose": "verify the Rust family tool dependency graph"
    },
    "Cargo.lock": {
      "command": "cargo test --locked",
      "purpose": "verify the pinned Rust dependency graph"
    },
    "rust-toolchain.toml": {
      "command": "just fast",
      "purpose": "verify the pinned MSRV toolchain, rustfmt, and Clippy lane"
    },
    "family.lock": {
      "command": "just contract",
      "purpose": "verify the family lock remains a fail-closed generated authority input"
    },
    "repos.manifest.toml": {
      "command": "just fast",
      "purpose": "verify the public family membership manifest"
    },
    "tools/": {
      "command": "just security",
      "purpose": "verify security adapters delegate to the canonical local lane"
    },
    ".github/": {
      "command": "bash scripts/ci-local.sh required",
      "purpose": "prove hosted lanes match local scripts"
    }
  }
}
